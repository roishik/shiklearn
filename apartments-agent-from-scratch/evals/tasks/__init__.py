"""Task definitions. seed_tasks.py holds the ~20-task seeded suite;
fixtures.py holds eval-only tools (not part of the shipped app) used by
a few of those tasks, e.g. the prompt-injection fixture tool."""
