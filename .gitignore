# Stray files that predate this repo and are not part of any project
How to Install XAMPP and WordPress Locally on Windows PC.html
How to Install XAMPP and WordPress Locally on Windows PC_files/
*.lnk
.DS_Store

# apartments-agent-from-scratch is now its OWN standalone repo, living
# nested inside this folder locally:
#   https://github.com/roishik/apartments-agent-from-scratch
# It is ignored here so the code is never duplicated across two GitHub
# repos and never tracked as a stray gitlink. Work on it from inside
# that folder, which has its own .git and its own remote.
apartments-agent-from-scratch/

# Same for data-gatekeeper-agent:
#   https://github.com/roishik/data-gatekeeper-agent
data-gatekeeper-agent/
